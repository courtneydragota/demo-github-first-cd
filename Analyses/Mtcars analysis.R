#view the first few rows
head(mtcars)

#calculate the mean mpg among all cars
mean_mpg <- mean(mtcars$mpg)

print(mean_mpg)


#Stage changes(write these in the terminal)
  #git add .
#Commit changes 
  #git commit -m "some message"
#Push changes 
  #git push origin main

#Pull changes
  #git pull
